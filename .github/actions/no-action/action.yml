name: "no action"

description: "no action description"

inputs:
  ainput:
    required: true
    default: 'default-action-input'

outputs:
  aoutput:
    description: 'Result of no action'

runs:
  using: "node16"
  main: "main.js"